import React from 'react';
import { FooterContainer, PartyButton } from './StyledComponents';

const Footer = () => {
  return (
    <FooterContainer>
      <PartyButton>JOIN THE PARTY</PartyButton>
    </FooterContainer>
  );
}

export default Footer;
